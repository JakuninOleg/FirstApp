class Arithmetic {
  static add(x, y) {
    return x + y;
  }

  static substract(x, y) {
    return x - y;
  }

  static multiply(x, y) {
    return x * y;
  }

  static divide(x, y) {
    return x / y;
  }
}

export default Arithmetic;
